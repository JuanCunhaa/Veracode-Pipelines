- task: PublishPipelineArtifact@1
  displayName: 'Upload artifact'
  inputs:
    targetPath: '$(Build.ArtifactStagingDirectory)/artifact'  # pasta ou arquivo a publicar
    artifact: 'analysisPack' # nome do artefato
    publishLocation: 'pipeline'
